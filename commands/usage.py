def run():
	print
	print "Supports a few different commands:"
	print "============================================================="
	print "setup:         sets up the server by initializing submodules."
	print "               and the database."
	print
	print "start:         starts the server."
	print
	print "clean:         destroys the database."
	print
	print
if __name__ == "main":
	run()